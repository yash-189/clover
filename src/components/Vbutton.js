import React from 'react'

const Vbutton = (props) => {
    
  return (
    <>
    <button type="button" className="btn vbtn " >{props.name}</button>
    
    </>
  )
}

export default Vbutton